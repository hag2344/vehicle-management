spring:
  application:
    name: vehicle-management
  profiles:
    default: local

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: false

  flyway:
    enabled: true
    baseline-on-migrate: false